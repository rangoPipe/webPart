export enum OwnerNameSpace {
    context = "context",
    detailListOwner = "detailListOwner",
    textAreaOwner = "textAreaOwner",
    messageBarOwner = "messageBarOwner",
    dialogOwner = "dialogOwner",
    commandBarOwner = "commandBarOwner",
}
