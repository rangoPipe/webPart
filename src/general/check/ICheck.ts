import { ICheckProps as ICheck } from "../../redux/reducers/general/check/ICheck";

export interface ICheckState extends ICheck {
    check:ICheck;
}

export interface ICheckProps extends ICheckState {
    
}